@model ASI.Basecode.Services.Models.BookViewModel

<style>
    /* Add CSS styles here */
    h1 {
        font-size: 24px;
        color: #333;
    }

    .details-container {
        border: 1px solid #ccc;
        padding: 20px;
        margin: 20px;
        background-color: #f9f9f9;
        border-radius: 5px;
    }

    dl.dl-horizontal {
        display: flex;
        flex-wrap: wrap;
    }

    dt {
        font-weight: bold;
        width: 120px;
    }

    dd {
        margin-left: 0;
        margin-bottom: 10px;
        flex-basis: calc(100% - 120px);
    }

    img.book-image {
        max-width: 300px;
        height: auto;
        margin-top: 10px;
    }
</style>

<h1>Book Details</h1>

<div class="details-container">
    <dl class="dl-horizontal">
        <dt>Image</dt>
        <dd><img src="@Model.Image" alt="Book Cover" class="book-image" /></dd>
        <dt>ID</dt>
        <dd>@Model.Id</dd>
        <dt>ISBN</dt>
        <dd>@Model.ISBN</dd>
        <dt>Title</dt>
        <dd>@Model.Name</dd>
        <dt>Authors</dt>
        <dd>
            @foreach (var authorName in Model.AuthorNames)
            {
                <div>@authorName</div>
            }
        </dd>
        <dt>Genres</dt>
        <dd>
            @foreach (var genreName in Model.GenreNames)
            {
                <div>@genreName</div>
            }
        </dd>
        <dt>Description</dt>
        <dd>@Model.Description</dd>
        <dt>Language</dt>
        <dd>@Model.Language</dd>
        <dt>Published On</dt>
        <dd>@Model.PublishedOn</dd>
        <dt>Page Count</dt>
        <dd>@Model.PageCount</dd>
    </dl>
</div>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-primary" }) |
    @Html.ActionLink("Back to List", "Index", new { @class = "btn btn-secondary" })
</p>
